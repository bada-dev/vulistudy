/**
 * Single source of truth for backend configuration.
 *
 * VuliStudy's Flask backend (the source of truth) is hosted on Render.
 * Every API call in the app goes through this base URL.
 */
export const API_BASE_URL = 'https://studybuddy-r616.onrender.com';
